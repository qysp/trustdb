module.exports = {
  /**
   * 'Equality' check.
   */
  'eq':  (a, b) => a == b,
  /**
   * 'Greater than' check.
   */
  'gt':  (a, b) => a > b,
  /**
   * 'Less than' check.
   */
  'lt':  (a, b) => a < b,
  /**
   * 'Strict equal' check.
   */
  'seq': (a, b) => a === b,
  /**
   * 'Greater than or equal' check.
   */
  'gte': (a, b) => a >= b,
  /**
   * 'Less than or equal' check.
   */
  'lte': (a, b) => a <= b,
};